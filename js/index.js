$(document).ready(function () {

    var windowWidth = $(window).width()
    var mouseX = 0;

    $(document).mousemove(function (e) {
        mouseX = e.pageX;
        if (mouseX < windowWidth / 2) {
            $(".box1").css("background-color", "#f1d42c");
        }
        if (mouseX > windowWidth / 2) {
            $(".box1").css("background-color", "purple");
        }
    })

    var windowWidth = $(window).width()
    var mouseX = 0;

    $(document).mousemove(function (e) {
        mouseX = e.pageX;
        if (mouseX < windowWidth / 2) {
            $(".box3").css("background-color", "rgb(224, 0, 0)");
        }
        if (mouseX > windowWidth / 2) {
            $(".box3").css("background-color", "#254EA1");
        }
    })

    var windowWidth = $(window).width()
    var mouseX = 0;

    $(document).mousemove(function (e) {
        mouseX = e.pageX;
        if (mouseX < windowWidth / 2) {
            $(".box5").css("background-color", "#254EA1");
        }
        if (mouseX > windowWidth / 2) {
            $(".box5").css("background-color", "purple");
        }
    })

    var windowWidth = $(window).width()
    var mouseX = 0;

    $(document).mousemove(function (e) {
        mouseX = e.pageX;
        if (mouseX < windowWidth / 2) {
            $(".box4").css("background-color", "#254EA1");
        }
        if (mouseX > windowWidth / 2) {
            $(".box4").css("background-color", "#f1d42c");
        }
    })

    var windowWidth = $(window).width()
    var mouseX = 0;

    $(document).mousemove(function (e) {
        mouseX = e.pageX;
        if (mouseX < windowWidth / 2) {
            $(".box8").css("background-color", "#f1d42c");
        }
        if (mouseX > windowWidth / 2) {
            $(".box8").css("background-color", "#254EA1");
        }
    })

    var windowWidth = $(window).width()
    var mouseX = 0;

    $(document).mousemove(function (e) {
        mouseX = e.pageX;
        if (mouseX < windowWidth / 2) {
            $(".circle3").css("background-color", "rgb(224, 0, 0)");
        }
        if (mouseX > windowWidth / 2) {
            $(".circle3").css("background-color", "#254EA1");
        }
    })
    var windowWidth = $(window).width()
    var mouseX = 0;

    $(document).mousemove(function (e) {
        mouseX = e.pageX;
        if (mouseX < windowWidth / 2) {
            $(".circle2").css("background-color", "#000000");
        }
        if (mouseX > windowWidth / 2) {
            $(".circle2").css("background-color", "rgb(224, 0, 0)");
        }
    })
    var windowWidth = $(window).width()
    var mouseX = 0;

    $(document).mousemove(function (e) {
        mouseX = e.pageX;
        if (mouseX < windowWidth / 2) {
            $(".circle6").css("background-color", "#000000");
        }
        if (mouseX > windowWidth / 2) {
            $(".circle6").css("background-color", "#2AAF5F");
        }
    })
    var windowWidth = $(window).width()
    var mouseX = 0;

    $(document).mousemove(function (e) {
        mouseX = e.pageX;
        if (mouseX < windowWidth / 2) {
            $(".circle7").css("background-color", "#EF8B9F");
        }
        if (mouseX > windowWidth / 2) {
            $(".circle7").css("background-color", "#000000");
        }
    })
    var windowWidth = $(window).width()
    var mouseX = 0;

    $(document).mousemove(function (e) {
        mouseX = e.pageX;
        if (mouseX < windowWidth / 2) {
            $(".circle").css("background-color", "#f1d42c");
        }
        if (mouseX > windowWidth / 2) {
            $(".circle").css("background-color", "#254EA1");
        }
    })
    var windowWidth = $(window).width()
    var mouseX = 0;

    $(document).mousemove(function (e) {
        mouseX = e.pageX;
        if (mouseX < windowWidth / 2) {
            $(".box7").css("background-color", "#2AAF5F");
        }
        if (mouseX > windowWidth / 2) {
            $(".box7").css("background-color", "rgb(224, 0, 0)");
        }
    })
    var windowWidth = $(window).width()
    var mouseX = 0;

    $(document).mousemove(function (e) {
        mouseX = e.pageX;
        if (mouseX < windowWidth / 2) {
            $(".box9").css("background-color", "rgb(224, 0, 0)");
        }
        if (mouseX > windowWidth / 2) {
            $(".box9").css("background-color", "#EF8B9F");
        }
    })
    var windowWidth = $(window).width()
    var mouseX = 0;

    $(document).mousemove(function (e) {
        mouseX = e.pageX;
        if (mouseX < windowWidth / 2) {
            $(".box10").css("background-color", "#2AAF5F");
        }
        if (mouseX > windowWidth / 2) {
            $(".box10").css("background-color", "rgb(224, 0, 0)");
        }
    })
})